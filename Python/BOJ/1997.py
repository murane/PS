import sys
r=sys.stdin.readline
n,w,b=map(int,r().split())
total_pan=[]
#각각의 박스를 입력받음
for _ in range(n):
    h=int(r())
    pan=[]
    for _ in range(h):
        line=r().strip()
        pan.append(line)
    total_pan.append(box)
boxex=[]
box=0
while total_pan:
    